document.getElementById('search-form')?.addEventListener('submit',async event=>{event.preventDefault();const button=event.submitter;button.disabled=true;button.textContent='Searching…';try{const data=await API.post('/api/businesses/search',{keyword:keyword.value,category:category.value,location:location.value,limit:Number(limit.value)||20});document.getElementById('search-results').innerHTML=data.leads.map(l=>`<tr><td>${escapeHtml(l.name)}</td><td>${escapeHtml(l.location)}</td><td>${l.rating||'—'} (${l.review_count||0})</td><td><span class="score">${l.score}</span></td><td>Saved</td></tr>`).join('')||'<tr><td colspan="5" class="empty">No businesses found.</td></tr>';toast(`${data.count} unique leads saved`)}catch(error){toast(error.message)}finally{button.disabled=false;button.textContent='Search businesses'}});
