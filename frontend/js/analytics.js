document.addEventListener('DOMContentLoaded',async()=>{try{const m=await API.get('/api/analytics/dashboard');document.getElementById('analytics').innerHTML=`<div class="grid stats"><div class="card stat"><h3>${m.total_leads}</h3><p>Total leads</p></div><div class="card stat"><h3>${m.qualified_leads}</h3><p>Qualified</p></div><div class="card stat"><h3>${m.high_score_leads}</h3><p>High score</p></div><div class="card stat"><h3>${m.outreach_activity}</h3><p>Outreach drafts/sends</p></div></div><div class="card" style="margin-top:18px"><h2 class="section-title">Pipeline by status</h2>${m.status_distribution.map(s=>`<p>${escapeHtml(s.status)} <span class="badge">${s.count}</span></p>`).join('')||'<p class="muted">No pipeline data yet.</p>'}</div>`}catch(e){toast(e.message)}});
