async function loadFollowups(){const items=await API.get('/api/followups');document.getElementById('followup-list').innerHTML=items.map(f=>`<tr><td>${escapeHtml(f.name)}</td><td>${escapeHtml(f.due_at)}</td><td>${escapeHtml(f.note||'')}</td><td><button class="button secondary" data-id="${f.id}">${f.status==='pending'?'Complete':'Completed'}</button></td></tr>`).join('')||'<tr><td colspan="4" class="empty">No scheduled follow-ups.</td></tr>';document.querySelectorAll('[data-id]').forEach(b=>b.onclick=async()=>{await API.put('/api/followups/'+b.dataset.id,{status:'completed'});loadFollowups()})}document.addEventListener('DOMContentLoaded',()=>loadFollowups().catch(e=>toast(e.message)));
